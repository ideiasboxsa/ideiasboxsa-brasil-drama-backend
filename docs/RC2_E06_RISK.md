# Risco E06 Backend

O risco residual é SQL/runtime: compilação Java não prova compatibilidade da consulta com o schema e os dados reais. Validar endpoint no PostgreSQL do ambiente antes de homologar.
